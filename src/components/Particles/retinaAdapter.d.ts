export function adaptEmitter(
  emitter: any,
  imageSize: { width: number; height: number },
): any;

export function adaptParticles(
  defaultOptions: any,
  imageSize: { width: number; height: number },
): any;
